# This file is auto-generated by tools/generate-travis-yaml.py.

before_install:
- if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo apt-get -y install libboost-dev; fi
- git submodule update --init --recursive
dist: trusty
language: generic
matrix:
  include:
  - addons:
      apt:
        packages:
        - g++-8
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-8 CXX=g++-8 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - g++-8
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-8 CXX=g++-8 std=c++2a
    os: linux
  - addons:
      apt:
        packages:
        - g++-9
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-9 CXX=g++-9 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - g++-9
        sources:
        - ubuntu-toolchain-r-test
    compiler: gcc
    env: CC=gcc-9 CXX=g++-9 std=c++2a
    os: linux
  - addons:
      apt:
        packages:
        - clang-6.0
        sources:
        - llvm-toolchain-trusty-6.0
    compiler: clang
    env: CC=clang-6.0 CXX=clang++-6.0 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - clang-6.0
        sources:
        - llvm-toolchain-trusty-6.0
    compiler: clang
    env: CC=clang-6.0 CXX=clang++-6.0 std=c++2a
    os: linux
  - addons:
      apt:
        packages:
        - clang-7.0
        sources:
        - llvm-toolchain-trusty-7.0
    compiler: clang
    env: CC=clang-7.0 CXX=clang++-7.0 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - clang-7.0
        sources:
        - llvm-toolchain-trusty-7.0
    compiler: clang
    env: CC=clang-7.0 CXX=clang++-7.0 std=c++2a
    os: linux
  - addons:
      apt:
        packages:
        - clang-8.0
        sources:
        - llvm-toolchain-trusty-8.0
    compiler: clang
    env: CC=clang-8.0 CXX=clang++-8.0 std=c++17
    os: linux
  - addons:
      apt:
        packages:
        - clang-8.0
        sources:
        - llvm-toolchain-trusty-8.0
    compiler: clang
    env: CC=clang-8.0 CXX=clang++-8.0 std=c++2a
    os: linux
script:
- mkdir build
- cd build
- STD=${std} cmake -DMANUALLY_SET_STANDARD_FLAGS=ON ..
- make
- ctest --output-on-failure
sudo: required
